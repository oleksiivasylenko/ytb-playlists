AGENTS expactations:

choosing language or texh stack:
1) use nodejs by default unless/or you think python would be preferable unless/or I say to use another language or tech stack.
2) use python for data science and machine learning tasks.
3) use js over typescript for small utility tools and typescript for larger projects

1) do not write comments in code but do not remove existing ones!
unlsee it is really describes specific usecases or behaviour
2) write readable and well separeted code

c# specific rules:
1) use var/let over strong type when it is possible.
2) prefer for over foreach

nodejs specific rules:
1) use typescript where it preffered. For small tools utilits use javascript.

python specific rules:
1) always create venv for new python projects
